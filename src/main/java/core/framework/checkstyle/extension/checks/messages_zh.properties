domain.layer.pkgs.disallow=领域层不能引用应用层、用户接口层、基础设施层。
domain.entities.pkgs.disallow=聚合、实体和值对象只能在同一上下文中引用。
domain.multiple.agg.disallow=一个上下文中只能有一个聚合根。

application.layer.pkgs.disallow=应用层不能引用 用户接口层、基础设施层。

userinterface.layer.pkgs.disallow=用户接口层不能引用基础设施层。

infrastructure.layer.pkgs.disallow=基础设施层不能引用 用户接口层。